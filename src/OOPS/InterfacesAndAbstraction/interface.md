In java Interface is a contract or rulebook,
it says , the class which implements me,
that class has to implement all
those methods which are available in Interface

Variables of Interfaces are public static final by default
Methods of Interfaces are public abstract by default

Interfaces not have objects because they have incomplete methods
