package ExceptionHandling;

public class ExceptionBasic {
    public static void main(String[] args) {
/*
        //this will give exception because we didn't handle the divide by zero case...
        int x = 10;
        int y = 0;
//        int z = x / y;
          System.out.println(x / y);

 */

        /*
        //this will not give u exception, output is infinity because we are dividing be double or float not integer
        //as the divisor get small, smaller the output will be large, larger.
        double x = 10;
        double y = 0.000000000001;
        long w = 0;
        System.out.println(x / w);

         */


//        System.out.println((int) 0.0 / 0.00); // will work and return NaN

//        int a = 10;
//        Double b = new Double(20 / 0.0);
//        System.out.println(b + " , " + b.isNaN()); // Infinity , false , in-case of Infinity it will return false
//        Double c = new Double((0.0 / 0.0));
//        System.out.println(c + " , " + c.isNaN()); // NaN , true , in-case of Nan  it will return true


    }
}
