#Hashing : It is a technique used to store and retrieve data in constant time(O(1)) by converting a key into an index using a hash function
Instead of searching data one by one , we jump directly to the location

Roll number -> student record
username -> user details
email - account information


### What is a HASH FUNCTION?

A hash function takes an input (key) and converts it into an integer value (hash).

### What is hashCode() in Java?

hashCode() is a method of Object class that returns an integer hash value of an object.
   public int hashCode()